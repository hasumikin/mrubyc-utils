CFLAGS = -mlongcalls -DMRBC_DEBUG
COMPONENT_ADD_INCLUDEDIRS := <%= mrubyc_src_dir.split('/').last %>
COMPONENT_SRCDIRS := <%= mrubyc_src_dir.split('/').last %> <%= mrubyc_src_dir.split('/').last %>/hal
