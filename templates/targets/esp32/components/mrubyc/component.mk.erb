CFLAGS = -mlongcalls -DMRBC_DEBUG
COMPONENT_ADD_INCLUDEDIRS := <%= @config["mrubyc_src_dir"].split('/').last %>
COMPONENT_SRCDIRS := <%= @config["mrubyc_src_dir"].split('/').last %> <%= @config["mrubyc_src_dir"].split('/').last %>/hal
